NOT DEPLOYED

Run locally using the instructions in docs/setup-guide.md

Quick start:
  cd src/backend && npm install && npm run dev   (Terminal 1)
  cd src/frontend && npm install && npm run dev  (Terminal 2)
  Open: http://localhost:5173

The application requires no external accounts, API keys, or databases.
All intelligence engines and demo data run locally in-process.
